dAu Correlations Unfold
=======================

This repository applies Bayesian inference techniques to two particle correlation functions in d+Au collisions.

This package uses the `emcee` python package for its MCMC method.